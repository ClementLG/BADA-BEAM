services:
  web:
    build: .
    #ports:
    #  - "127.0.0.1:${PORT:-5000}:${PORT:-5000}" #uncomment this line if you want to bind the port. For security reason, it is recommended to use a reverse proxy in production.
    env_file:
      - .env

    restart: unless-stopped
